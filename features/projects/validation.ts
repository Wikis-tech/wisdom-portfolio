import { z } from "zod";
export const projectSchema=z.object({title:z.string().trim().min(2).max(120),slug:z.string().trim().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).max(140),shortDescription:z.string().trim().max(500).optional(),year:z.coerce.number().int().min(2000).max(2100).optional(),client:z.string().trim().max(120).optional(),role:z.string().trim().max(160).optional(),status:z.enum(["shipped","live","in_development","prototype","experiment","concept","archived"]),visibility:z.enum(["public","private"]),featured:z.boolean(),confidential:z.boolean(),liveUrl:z.union([z.string().url(),z.literal("")]).optional(),githubUrl:z.union([z.string().url(),z.literal("")]).optional()});
export const blockSchema=z.object({blockType:z.enum(["heading","paragraph","image","gallery","video","quote","stats","two_column","full_width_image","technology","before_after","embed","spacer","cta"]),content:z.string().max(12000)});