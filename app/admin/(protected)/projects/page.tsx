import Link from "next/link";import {createClient} from "@/lib/supabase/server";
export default async function ProjectsPage(){const s=await createClient();const {data:p}=await s.from("projects").select("id,title,slug,status,featured,content_state,confidential").is("deleted_at",null).order("updated_at",{ascending:false});return <main className="mx-auto max-w-[1440px]"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">CONTENT / PROJECTS</p><h1 className="mt-3 text-4xl font-semibold">Projects</h1></div><Link href="/admin/projects/new" className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">+ New Project</Link></div><div className="mt-8 overflow-hidden rounded-2xl border border-white/[.08]">{p?.length?p.map(x=><div key={x.id} className="grid gap-3 border-b border-white/[.06] px-5 py-4 last:border-0 md:grid-cols-[1fr_160px_120px_90px] md:items-center"><div><p className="font-medium">{x.title}</p><p className="mt-1 text-xs text-white/30">/{x.slug}{x.confidential?" · confidential":""}</p></div><span className="text-sm text-white/50">{x.status.replaceAll("_"," ")}</span><span className={`w-fit rounded-full px-2.5 py-1 text-[11px] ${x.content_state==="published"?"bg-emerald-400/10 text-emerald-300":"bg-amber-400/10 text-amber-200"}`}>{x.content_state}</span><Link href={`/admin/projects/${x.id}`} className="text-sm text-[#8097ff]">Edit →</Link></div>):<div className="p-14 text-center text-sm text-white/40">No projects yet.</div>}</div></main>}