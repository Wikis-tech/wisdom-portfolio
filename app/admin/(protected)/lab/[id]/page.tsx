import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {saveExperiment,trashExperiment} from "../actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function ExperimentEditor({params}:{params:Promise<{id:string}>}){const {id}=await params,s=await createClient();const {data:x}=await s.from("experiments").select("*").eq("id",id).is("deleted_at",null).single();if(!x)notFound();return <main className="mx-auto max-w-5xl"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">LAB / EDIT</p><h1 className="mt-3 text-4xl font-semibold">{x.title}</h1><form action={saveExperiment} className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><input type="hidden" name="id" value={id}/><div className="grid gap-3 md:grid-cols-2"><input name="title" defaultValue={x.title} className={input}/><input name="slug" defaultValue={x.slug} className={input}/><textarea name="description" defaultValue={x.description??""} rows={3} className={`${input} md:col-span-2`}/><select name="status" defaultValue={x.status} className={input}>{["experiment","prototype","in_development","published","archived"].map(v=><option key={v} value={v}>{v.replaceAll("_"," ")}</option>)}</select><select name="visibility" defaultValue={x.visibility} className={input}>{["draft","public","private","archived"].map(v=><option key={v}>{v}</option>)}</select><input name="technology" defaultValue={(x.technology??[]).join(", ")} className={input}/><input name="date" type="date" defaultValue={x.experiment_date??""} className={input}/><input name="imageUrl" type="url" defaultValue={x.image_url??""} placeholder="Image URL" className={input}/><input name="videoUrl" type="url" defaultValue={x.video_url??""} placeholder="Video URL" className={input}/><input name="githubUrl" type="url" defaultValue={x.github_url??""} placeholder="GitHub URL" className={input}/><input name="demoUrl" type="url" defaultValue={x.demo_url??""} placeholder="Demo URL" className={input}/><textarea name="caseStudy" defaultValue={x.case_study??""} rows={8} placeholder="Case study / experiment notes" className={`${input} md:col-span-2`}/><input name="sortOrder" type="number" defaultValue={x.sort_order} className={input}/><label className="self-center text-sm text-white/50"><input name="featured" type="checkbox" defaultChecked={x.featured} className="mr-2"/>Featured</label></div><div className="mt-5 flex justify-end"><button className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">Save Experiment</button></div></form><form action={trashExperiment} className="mt-6 flex justify-end"><input type="hidden" name="id" value={id}/><button className="text-sm text-red-300/70">Move to Trash</button></form></main>}