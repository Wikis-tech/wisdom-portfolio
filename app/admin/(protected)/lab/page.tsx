import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {createExperiment,saveCraft} from "./actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function LabAdmin(){const s=await createClient();const [{data:items},{data:craft}]=await Promise.all([s.from("experiments").select("id,title,slug,status,visibility,featured,updated_at").is("deleted_at",null).order("sort_order"),s.from("craft_settings").select("*").eq("singleton_key","default").single()]);return <main className="mx-auto max-w-6xl"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">CONTENT / LAB</p><h1 className="mt-3 text-4xl font-semibold">Lab & experiments</h1></div><Link href="/lab" className="text-sm text-white/50">View Lab ↗</Link></div><section className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><h2 className="text-lg font-semibold">New experiment</h2><form action={createExperiment} className="mt-5 grid gap-3 md:grid-cols-2"><input name="title" required placeholder="Title" className={input}/><input name="slug" required placeholder="slug" className={input}/><textarea name="description" placeholder="Description" rows={3} className={`${input} md:col-span-2`}/><select name="status" className={input}><option value="experiment">Experiment</option><option value="prototype">Prototype</option><option value="in_development">In development</option><option value="published">Published</option><option value="archived">Archived</option></select><select name="visibility" className={input}><option value="draft">Draft</option><option value="public">Public</option><option value="private">Private</option><option value="archived">Archived</option></select><input name="technology" placeholder="Technologies, comma separated" className={input}/><input name="imageUrl" type="url" placeholder="Image URL" className={input}/><input name="sortOrder" type="number" defaultValue={0} className={input}/><label className="self-center text-sm text-white/50"><input name="featured" type="checkbox" className="mr-2"/>Featured</label><button className="md:col-span-2 rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">+ Create Experiment</button></form></section><section className="mt-6 overflow-hidden rounded-2xl border border-white/[.08]">{items?.length?items.map(x=><div key={x.id} className="grid gap-3 border-b border-white/[.06] px-5 py-4 last:border-0 md:grid-cols-[1fr_150px_120px_100px] md:items-center"><div><p className="font-medium">{x.title}</p><p className="mt-1 text-xs text-white/30">/{x.slug}</p></div><span className="text-sm text-white/45">{x.status.replaceAll("_"," ")}</span><span className="text-sm text-white/45">{x.visibility}</span><Link href={`/admin/lab/${x.id}`} className="text-sm text-[#8097ff]">Edit →</Link></div>):<div className="p-12 text-center text-sm text-white/35">No experiments yet.</div>}</section><section className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><p className="text-xs font-semibold tracking-[.16em] text-white/30">AI / CRAFT</p><h2 className="mt-2 text-xl font-semibold">CRAFT framework</h2><form action={saveCraft} className="mt-5 grid gap-3 md:grid-cols-2"><input name="heading" defaultValue={craft?.heading??"CRAFT"} className={input}/><input name="message" defaultValue={craft?.message??""} className={input}/><input name="context" defaultValue={craft?.context_label??"Context"} className={input}/><input name="role" defaultValue={craft?.role_label??"Role"} className={input}/><input name="action" defaultValue={craft?.action_label??"Action"} className={input}/><input name="format" defaultValue={craft?.format_label??"Format"} className={input}/><input name="tone" defaultValue={craft?.tone_label??"Tone / Limits"} className={input}/><input name="usageItems" defaultValue={(craft?.usage_items??[]).join(", ")} placeholder="Research, Development..." className={input}/><label className="text-sm text-white/50"><input name="enabled" type="checkbox" defaultChecked={craft?.enabled} className="mr-2"/>Show publicly</label><button className="rounded-xl border border-white/10 px-4 py-2.5 text-sm">Save CRAFT</button></form></section></main>}