import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {createService} from "./actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function ServicesAdmin(){const s=await createClient();const {data:items}=await s.from("services").select("id,title,slug,short_description,enabled,featured,sort_order").is("deleted_at",null).order("sort_order");return <main className="mx-auto max-w-6xl"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">BUSINESS / SERVICES</p><h1 className="mt-3 text-4xl font-semibold">Services</h1></div><Link href="/services" className="text-sm text-white/50">View Services ↗</Link></div><section className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><h2 className="text-lg font-semibold">New service</h2><form action={createService} className="mt-5 grid gap-3 md:grid-cols-2"><input name="title" required placeholder="Service title" className={input}/><input name="slug" required placeholder="service-slug" className={input}/><textarea name="shortDescription" required rows={2} placeholder="Short description" className={`${input} md:col-span-2`}/><input name="startingPrice" type="number" min="0" placeholder="Starting price" className={input}/><input name="currency" defaultValue="NGN" className={input}/><input name="sortOrder" type="number" defaultValue={0} className={input}/><div className="flex items-center gap-4 text-sm text-white/50"><label><input name="featured" type="checkbox" className="mr-2"/>Featured</label><label><input name="enabled" type="checkbox" defaultChecked className="mr-2"/>Enabled</label></div><button className="md:col-span-2 rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">+ Create Service</button></form></section><div className="mt-6 overflow-hidden rounded-2xl border border-white/[.08]">{items?.length?items.map(x=><div key={x.id} className="grid gap-3 border-b border-white/[.06] px-5 py-4 last:border-0 md:grid-cols-[1fr_120px_120px_90px] md:items-center"><div><p className="font-medium">{x.title}</p><p className="mt-1 text-xs text-white/30">{x.short_description}</p></div><span className="text-sm text-white/45">{x.enabled?"Enabled":"Hidden"}</span><span className="text-sm text-white/45">{x.featured?"Featured":"—"}</span><Link href={`/admin/services/${x.id}`} className="text-sm text-[#8097ff]">Edit →</Link></div>):<div className="p-12 text-center text-sm text-white/35">No services yet.</div>}</div></main>}