import {createClient} from "@/lib/supabase/server";import {createTechnology,deleteTechnology,saveTechnology} from "./actions";const cats=["Frontend","Backend/Data","Infrastructure","Creative","Business","AI"];const input="rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function TechnologiesPage(){const s=await createClient();const {data:items}=await s.from("technologies").select("*").order("category").order("sort_order");return <main className="mx-auto max-w-6xl"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">SITE / TECHNOLOGIES</p><h1 className="mt-3 text-4xl font-semibold">Technologies</h1><form action={createTechnology} className="mt-8 grid gap-3 rounded-2xl border border-white/[.08] bg-white/[.02] p-5 md:grid-cols-2 xl:grid-cols-[1fr_180px_1fr_1fr_90px_auto]"><input name="name" required placeholder="Technology" className={input}/><select name="category" className={input}>{cats.map(c=><option key={c}>{c}</option>)}</select><input name="iconUrl" type="url" placeholder="Icon URL" className={input}/><input name="websiteUrl" type="url" placeholder="Website URL" className={input}/><input name="sortOrder" type="number" defaultValue={0} className={input}/><button className="rounded-xl bg-white px-4 text-sm font-semibold text-black">+ Add</button></form><div className="mt-6 space-y-2">{items?.map(x=><form action={saveTechnology} key={x.id} className="grid gap-2 rounded-xl border border-white/[.07] p-3 md:grid-cols-2 xl:grid-cols-[1fr_180px_1fr_1fr_80px_90px_auto]"><input type="hidden" name="id" value={x.id}/><input name="name" defaultValue={x.name} className={input}/><select name="category" defaultValue={x.category} className={input}>{cats.map(c=><option key={c}>{c}</option>)}</select><input name="iconUrl" type="url" defaultValue={x.icon_url??""} className={input}/><input name="websiteUrl" type="url" defaultValue={x.website_url??""} className={input}/><input name="sortOrder" type="number" defaultValue={x.sort_order} className={input}/><label className="self-center text-xs text-white/50"><input name="visible" type="checkbox" defaultChecked={x.is_visible} className="mr-1"/>Visible</label><div className="flex items-center gap-3"><button className="text-xs text-[#8ea0ff]">Save</button><button formAction={deleteTechnology} className="text-xs text-red-300/70">Delete</button></div></form>)}</div></main>}