"use server";
import {revalidatePath} from "next/cache";import {redirect} from "next/navigation";import {requireCmsUser} from "@/lib/auth/require-cms-user";import {createClient} from "@/lib/supabase/server";
const statuses=new Set(["unread","read","replied","archived"]);
export async function updateMessageStatus(fd:FormData){const u=await requireCmsUser(),s=await createClient(),id=String(fd.get("id")),status=String(fd.get("status"));if(!statuses.has(status))throw new Error("Invalid status.");await s.from("contact_messages").update({status,archived_at:status==="archived"?new Date().toISOString():null,updated_at:new Date().toISOString()}).eq("id",id);await s.from("activity_logs").insert({user_id:u.id,action:`Message marked ${status}`,entity_type:"message",entity_id:id});revalidatePath("/admin/messages");revalidatePath(`/admin/messages/${id}`);revalidatePath("/admin")}
export async function deleteMessage(fd:FormData){const u=await requireCmsUser();if(u.role!=="admin")throw new Error("Admin access required.");const s=await createClient(),id=String(fd.get("id"));await s.from("contact_messages").delete().eq("id",id);await s.from("activity_logs").insert({user_id:u.id,action:"Message deleted permanently",entity_type:"message",entity_id:id});revalidatePath("/admin/messages");revalidatePath("/admin");redirect("/admin/messages")}