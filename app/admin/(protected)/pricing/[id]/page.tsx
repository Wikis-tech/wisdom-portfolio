import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {savePackage,trashPackage} from "../actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function PricingEditor({params}:{params:Promise<{id:string}>}){const {id}=await params,s=await createClient();const {data:x}=await s.from("pricing_packages").select("*").eq("id",id).is("deleted_at",null).single();if(!x)notFound();return <main className="mx-auto max-w-5xl"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">PRICING / EDIT</p><h1 className="mt-3 text-4xl font-semibold">{x.package_name}</h1><form action={savePackage} className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><input type="hidden" name="id" value={id}/><div className="grid gap-3 md:grid-cols-2"><input name="name" defaultValue={x.package_name} className={input}/><input name="subtitle" defaultValue={x.subtitle??""} className={input}/><input name="currency" defaultValue={x.currency} className={input}/><input name="price" type="number" min="0" defaultValue={x.starting_price??""} className={input}/><textarea name="description" defaultValue={x.description??""} rows={3} className={`${input} md:col-span-2`}/><textarea name="features" defaultValue={(x.included_features??[]).join("\n")} rows={6} className={`${input} md:col-span-2`}/><input name="ctaText" defaultValue={x.cta_text} className={input}/><input name="ctaUrl" defaultValue={x.cta_url} className={input}/><input name="sortOrder" type="number" defaultValue={x.sort_order} className={input}/><div className="flex flex-wrap items-center gap-4 text-sm text-white/50"><label><input name="customQuote" type="checkbox" defaultChecked={x.custom_quote} className="mr-2"/>Custom Quote</label><label><input name="featured" type="checkbox" defaultChecked={x.featured} className="mr-2"/>Featured</label><label><input name="visible" type="checkbox" defaultChecked={x.visibility} className="mr-2"/>Visible</label></div></div><div className="mt-5 flex justify-end"><button className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">Save Package</button></div></form><form action={trashPackage} className="mt-6 flex justify-end"><input type="hidden" name="id" value={id}/><button className="text-sm text-red-300/70">Move to Trash</button></form></main>}