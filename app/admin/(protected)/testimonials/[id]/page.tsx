import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {archiveTestimonial,saveTestimonial} from "../actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function TestimonialEditor({params}:{params:Promise<{id:string}>}){const {id}=await params,s=await createClient();const [{data:x},{data:projects}]=await Promise.all([s.from("testimonials").select("*").eq("id",id).is("deleted_at",null).single(),s.from("projects").select("id,title").is("deleted_at",null).order("title")]);if(!x)notFound();return <main className="mx-auto max-w-5xl"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">TESTIMONIAL / EDIT</p><h1 className="mt-3 text-4xl font-semibold">{x.name}</h1><form action={saveTestimonial} className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><input type="hidden" name="id" value={id}/><div className="grid gap-3 md:grid-cols-2"><input name="name" defaultValue={x.name} className={input}/><input name="position" defaultValue={x.position??""} className={input}/><input name="company" defaultValue={x.company??""} className={input}/><input name="photoUrl" type="url" defaultValue={x.photo_url??""} className={input}/><select name="projectId" defaultValue={x.project_id??""} className={input}><option value="">No linked project</option>{projects?.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select><input name="sortOrder" type="number" defaultValue={x.sort_order} className={input}/><textarea name="quote" defaultValue={x.quote} rows={7} className={`${input} md:col-span-2`}/><div className="flex gap-4 text-sm text-white/50"><label><input name="featured" type="checkbox" defaultChecked={x.featured} className="mr-2"/>Featured</label><label><input name="visible" type="checkbox" defaultChecked={x.visibility} className="mr-2"/>Published</label></div></div><div className="mt-5 flex justify-end"><button className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">Save Testimonial</button></div></form><form action={archiveTestimonial} className="mt-6 flex justify-end"><input type="hidden" name="id" value={id}/><button className="text-sm text-red-300/70">Archive testimonial</button></form></main>}