import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {createTestimonial} from "./actions";const input="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function TestimonialsPage(){const s=await createClient();const [{data:items},{data:projects}]=await Promise.all([s.from("testimonials").select("id,name,position,company,featured,visibility,sort_order,updated_at").is("deleted_at",null).order("sort_order"),s.from("projects").select("id,title").is("deleted_at",null).order("title")]);return <main className="mx-auto max-w-6xl"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">SITE / TESTIMONIALS</p><h1 className="mt-3 text-4xl font-semibold">Testimonials</h1><p className="mt-2 text-sm text-white/40">Only publish testimonials you actually received. Hidden entries never appear publicly.</p><section className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><h2 className="text-lg font-semibold">Add testimonial</h2><form action={createTestimonial} className="mt-5 grid gap-3 md:grid-cols-2"><input name="name" required placeholder="Name" className={input}/><input name="position" placeholder="Position" className={input}/><input name="company" placeholder="Company" className={input}/><input name="photoUrl" type="url" placeholder="Photo URL (optional)" className={input}/><select name="projectId" className={input}><option value="">No linked project</option>{projects?.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select><input name="sortOrder" type="number" defaultValue={0} className={input}/><textarea name="quote" required minLength={10} maxLength={1500} rows={5} placeholder="Exact testimonial quote" className={`${input} md:col-span-2`}/><div className="flex gap-4 text-sm text-white/50"><label><input name="featured" type="checkbox" className="mr-2"/>Featured</label><label><input name="visible" type="checkbox" className="mr-2"/>Published</label></div><button className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">+ Add Testimonial</button></form></section><div className="mt-6 overflow-hidden rounded-2xl border border-white/[.08]">{items?.length?items.map(x=><div key={x.id} className="grid gap-3 border-b border-white/[.06] px-5 py-4 last:border-0 md:grid-cols-[1fr_160px_120px_90px] md:items-center"><div><p className="font-medium">{x.name}</p><p className="mt-1 text-xs text-white/30">{[x.position,x.company].filter(Boolean).join(" · ")||"No role/company"}</p></div><span className="text-sm text-white/45">{x.visibility?"Published":"Hidden"}</span><span className="text-sm text-white/45">{x.featured?"Featured":"—"}</span><Link href={`/admin/testimonials/${x.id}`} className="text-sm text-[#8097ff]">Edit →</Link></div>):<div className="p-14 text-center text-sm text-white/35">No testimonials yet — the public section will stay hidden.</div>}</div></main>}