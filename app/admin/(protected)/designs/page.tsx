import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {createDesign,createDesignCategory} from "./actions";
const input="rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm";
export default async function DesignsPage(){const s=await createClient();const [{data:items},{data:cats}]=await Promise.all([s.from("designs").select("id,title,client,year,featured,visibility,updated_at,design_categories(name)").is("deleted_at",null).order("sort_order"),s.from("design_categories").select("id,name,slug").eq("is_visible",true).order("sort_order")]);return <main className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">CONTENT / DESIGN ARCHIVE</p><h1 className="mt-3 text-4xl font-semibold">Design Archive</h1><p className="mt-2 text-sm text-white/45">Individual creative work without forcing every piece into a full case study.</p></div><Link href="/archive" className="text-sm text-white/50">View Archive ↗</Link></div><section className="mt-8 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><h2 className="text-lg font-semibold">Add design</h2><form action={createDesign} className="mt-5 grid gap-3 md:grid-cols-2"><input name="title" required placeholder="Title" className={input}/><input name="client" placeholder="Client" className={input}/><input name="year" type="number" placeholder="Year" className={input}/><select name="categoryId" className={input}><option value="">No category</option>{cats?.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input name="imageUrl" type="url" required placeholder="Primary image URL" className="md:col-span-2 rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm"/><textarea name="description" rows={3} placeholder="Description" className="md:col-span-2 rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm"/><input name="tags" placeholder="Tags, comma separated" className={input}/><select name="visibility" className={input}><option value="public">Public</option><option value="private">Private</option></select><input name="sortOrder" type="number" defaultValue={0} className={input}/><label className="self-center text-sm text-white/50"><input name="featured" type="checkbox" className="mr-2"/>Featured</label><button className="md:col-span-2 rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black">+ Add Design</button></form></section><section className="mt-6 rounded-2xl border border-white/[.08] bg-white/[.02] p-6"><h2 className="text-lg font-semibold">Categories</h2><form action={createDesignCategory} className="mt-4 grid gap-3 md:grid-cols-[1fr_1fr_100px_auto]"><input name="name" required placeholder="Category name" className={input}/><input name="slug" required placeholder="category-slug" className={input}/><input name="sortOrder" type="number" defaultValue={0} className={input}/><button className="rounded-xl border border-white/10 px-4 text-sm">Add</button></form></section><div className="mt-6 overflow-hidden rounded-2xl border border-white/[.08]">{items?.length?items.map(x=><div key={x.id} className="grid gap-3 border-b border-white/[.06] px-5 py-4 last:border-0 md:grid-cols-[1fr_180px_100px_100px_90px] md:items-center"><div><p className="font-medium">{x.title}</p><p className="mt-1 text-xs text-white/30">{x.client||"No client"} · {x.year||"—"}</p></div><span className="text-sm text-white/45">{(x.design_categories as {name?:string}|null)?.name||"Uncategorised"}</span><span className="text-sm text-white/45">{x.visibility}</span><span className="text-sm text-white/45">{x.featured?"Featured":"—"}</span><Link href={`/admin/designs/${x.id}`} className="text-sm text-[#8097ff]">Edit →</Link></div>):<div className="p-14 text-center text-sm text-white/35">No designs yet.</div>}</div></main>}