import {SimplePage} from "@/components/portfolio/simple-page";export default function Contact(){return <SimplePage eyebrow="CONTACT" title="Have an idea? Let's build something useful." copy="Whether it's a product, website, campaign, experiment or something that doesn't quite fit a category yet, this is where the conversation starts. The secure contact inbox is connected in the leads/CRM phase."/>}