import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {PublicNav} from "@/components/portfolio/public-nav";
export default async function ServicesPage(){const s=await createClient();const {data:items}=await s.from("services").select("id,title,slug,short_description,full_description,icon,featured_image_url,starting_price,currency,cta_label,cta_url,featured,service_pricing_links(pricing_package_id,pricing_packages(package_name,starting_price,currency,custom_quote,visibility))").eq("enabled",true).is("deleted_at",null).order("sort_order");return <main className="min-h-screen bg-[#06070b] text-white"><PublicNav/><section className="mx-auto w-[min(1320px,calc(100%-40px))] py-20"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">SERVICES</p><h1 className="mt-5 max-w-4xl text-5xl font-semibold tracking-[-.05em] sm:text-7xl">What can we build?</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/45">Some problems need code. Others need design. The interesting ones usually need both.</p>{items?.length?<div className="mt-14 grid gap-4 md:grid-cols-2">{items.map((x,i)=><article key={x.id} className={`relative overflow-hidden rounded-3xl border border-white/[.08] bg-white/[.025] p-6 sm:p-8 ${i===0&&x.featured?"md:col-span-2":""}`}>{x.icon&&<p className="text-xl text-[#8097ff]">{x.icon}</p>}<h2 className="mt-8 text-3xl font-semibold tracking-[-.04em]">{x.title}</h2><p className="mt-4 max-w-2xl text-base leading-7 text-white/50">{x.full_description||x.short_description}</p>{x.starting_price!=null&&<p className="mt-6 text-sm text-white/35">From {x.currency==="NGN"?"₦":x.currency+" "}{Number(x.starting_price).toLocaleString()}</p>}<Link href={x.cta_url||"/contact"} className="mt-7 inline-block text-sm text-white/70">{x.cta_label||"Let's talk ↗"}</Link></article>)}</div>:<div className="mt-14 rounded-3xl border border-dashed border-white/10 p-14 text-center text-sm text-white/35">Services will appear here once enabled in the CMS.</div>}</section></main>}