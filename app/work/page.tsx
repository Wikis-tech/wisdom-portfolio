import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {PublicNav} from "@/components/portfolio/public-nav";
export default async function WorkPage(){const s=await createClient();const {data:p}=await s.from("project_publications").select("project_id,slug,title,short_description,status,confidential,snapshot,published_at").order("sort_order").order("published_at",{ascending:false});return <main className="min-h-screen bg-[#06070b] text-white"><PublicNav/><section className="mx-auto w-[min(1320px,calc(100%-40px))] py-20"><p className="text-xs font-semibold tracking-[.18em] text-[#6f8cff]">WORK</p><h1 className="mt-5 max-w-4xl text-5xl font-semibold tracking-[-.05em] sm:text-7xl">Products, systems and visual work built around real problems.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/45">Software, product thinking, creative communication and experiments — presented as one connected practice.</p><div className="mt-14 grid gap-4 md:grid-cols-2">{p?.length?p.map((x,i)=><Link key={x.project_id} href={`/work/${x.slug}`} className={`rounded-3xl border border-white/[.08] bg-white/[.025] p-6 transition hover:bg-white/[.045] ${i%5===0?"md:col-span-2 md:min-h-72":""}`}><p className="text-xs uppercase tracking-[.15em] text-white/30">{x.status.replaceAll("_"," ")}{x.confidential?" · confidential":""}</p><h2 className="mt-16 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">{x.title}</h2><p className="mt-3 max-w-xl text-sm leading-6 text-white/45">{x.short_description}</p></Link>):<div className="md:col-span-2 rounded-3xl border border-dashed border-white/10 p-14 text-center text-sm text-white/35">No published projects yet. Publish one from the Admin CMS and it will appear here automatically.</div>}</div></section></main>}